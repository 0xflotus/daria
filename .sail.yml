tasks:
  build:
    image: node:carbon
    command:
      - yarn
    args:
      - install
  test:
    image: node:carbon
    command:
      - yarn
    args:
      - test
workflow:  
  - build  
  - lint    
  - test
  
